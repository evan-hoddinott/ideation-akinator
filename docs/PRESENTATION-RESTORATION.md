# Sage and internet journey restoration

The September 13 workflow release left the Sage's event dialogue undisplayed, froze era progression during concepts and finalization, and placed the newer media pages outside the pixel transition. The user requested these corrections while preserving the improved workflow and Webfishing presentation contract.

## Changes

- `SageBanter` displays contextual greetings and existing personality events across the workflow. Optional Chat rotates era and stage remarks. It never submits an answer or advances a workflow step. Main dialogue owns speech while delivering a question; the aside appears once without replaying afterward. Calm mode shows it immediately. A separate final-plan event replaces an incorrect instruction to compare concepts after the final plan was saved.
- `VerticalWorld` separates menu pause from disabled background interactions. Concepts and finalization keep the journey clock running. Hidden media remain paused and inert. Menus still pause the journey itself.
- `PixelEraLayer` keeps both incoming and outgoing media pages mounted during a crossing. Its mask uses the same rank ordering, reveal count, resolution and direction as the 3D compositor. Media artwork, thumbnails, logos and playing video sample a shared render grid. Functional text and controls remain crisp. This is actual raster sampling, not just pixel borders.
- Concept, feature, finalization and score windows leave the world visible around them. Their layouts reserve room for the conversation area at desktop and phone sizes.

## Verification, September 14

Local browser checks used the configured user-local agent-browser with sandboxed Chromium and software WebGL. Evidence is under `artifacts/presentation-restoration/`, ignored by Git.

- A saved token-free demo with completed broad research was resumed at concept selection, finalized through the actual UI, and completed focused research, plan generation, final printing and results. It was not a fresh paid-model run.
- `journey-proof.json` contains 312 samples with eras 9, 10, 11, 12 and 13 while background interactions remained disabled. Both media pages were present during the 10-to-11 crossing. Menu pause retained the same era and merge values. No workflow wait was added to let the scenery catch up.
- `milestone-travel.webm` records this browser session, including setup and pause checks. Use sampled browser state for progression evidence; this CLI capture is not a precise frame-rate benchmark.
- `pixel-crossing.png` is a held development preview of the 10-to-11 midpoint. The complete page participates in the sparse-to-dense transition while the Sage and journal stay readable.
- Chat, a topic edit and keyboard selection of a related cat clip left the main dialogue in its completed response state. The new video played. No horizontal overflow was observed at 1440 or 390 pixels.
- Inspected desktop concept, finalization, media, transition and result screenshots, plus phone problem, feature-workshop and result screenshots. Browser page errors were empty. The check did not repeat every earlier animation, all fourteen era compositions, paid generation or PDF content verification.
- Svelte check: zero errors and warnings. Nineteen targeted era, pixel-gradient, personality and RPG dialogue tests passed. Changed-file ESLint and formatting checks passed. Production build passed.

## Release

Publication is pending final container and public-browser verification. Rollback image: `ideation-akinator:before-sage-restoration-20260914`, image `sha256:873d49ae3405d0845f52351b76045d0f465a77a7b5e2c563f7a3c2bbadb2f41e`.
