# Workflow implementation and release record

The approved W-01 through W-14 functional redesign is implemented. The September 13 release still had presentation regressions: disconnected Sage reactions, paused late-era travel, and media layers outside the pixel transition. See [presentation restoration](PRESENTATION-RESTORATION.md) for the correction and its verification. The visual correction follows the user's Webfishing direction: the existing low-poly Sage/world, Tomo bitmap lettering, stepped pixel frames, hard shadows and native-resolution sprites. This record separates observed behavior from generated estimates. The attached assignment is a rubric reference; this is not a completed student submission.

Application revision `947e156` is deployed at `https://idea.battery.rip`. The public health/authentication/container verifier passes. Final browser evidence is recorded below. See [verification](WORKFLOW-VERIFICATION.md), [approved scope](WORKFLOW-REDESIGN-PROPOSAL.md), [asset credits](WORKFLOW-ASSET-SOURCES.md) and [deployment](DEPLOYMENT.md).

## Delivered workflow

| Requirement          | Final behavior                                                                                                                                                                                                                                                                   |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W-01 Opening/problem | Direct quest journal with editable related problems and persistent primary action.                                                                                                                                                                                               |
| W-02 Tags            | Shared themed autocomplete, keyboard selection, removable chips, suggested technology/research tags and persistent dismissal. Explicit exclusions remain distinct from preferences.                                                                                              |
| W-03 Originality     | Pixel slider with visible value and keyboard operation.                                                                                                                                                                                                                          |
| W-04 Spend           | Animated budget meter, exact amount, presets and themed optional production checkbox. Empty/negative/excessive amounts prevent research.                                                                                                                                         |
| W-05 Interview       | All answer options on one page, stable dialogue identity, visible pending selection and duplicate-submit guard. Custom answers, skip, unknown and early completion remain available.                                                                                             |
| W-06 Broad research  | One briefing, expandable evidence, direct interview action and continuous printer feed/reach/handoff.                                                                                                                                                                            |
| W-07 Concepts        | Four concise nine-field rough designs, Purl's keyboard performance, physical monitor swivel, mail and comparison in one pixel desktop. Saved portfolios do not replay the performance.                                                                                           |
| W-08 Fourth option   | Contained dismissible virus gag, corrupted sender and clear overage. Only cash budget is exempt; explicit deadline and excluded-technology checks also apply to the fourth.                                                                                                      |
| W-09 Configuration   | Per-concept Build now/Later/Leave out drafts, dependency checks, custom features, editable descriptions, reset and confirmation. Changes invalidate finalization and mark initial estimates provisional.                                                                         |
| W-10 Finalization    | Focused research proceeds into one final plan automatically. Material conflicts explain the affected choice and offer revision. Completed research survives plan failure. Cancellation and full-input identities prevent stale responses publishing. Only the final plan prints. |
| W-11 Results         | Full-screen animated factor/modifier tally with a small Sage portrait. Project feasibility stays separate from game points. Report/PDF work independently of score success.                                                                                                      |
| W-12 YouTube         | Ten distinct local clips, matched posters, stable per-run playlist, working controls and aligned pixel-rendered bezel. Unrelated box removed.                                                                                                                                    |
| W-13 Social feeds    | Recognizable Purlgram/Purltok feeds, distinct media, one active clip, local interaction feedback and scrolling. Hidden desktop backgrounds are inert.                                                                                                                            |
| W-14 Assets          | Wand/hat cursors, flame animation, contextual Neko/Purl poses and varied local cat media. Attribution and adaptation records accompany assets.                                                                                                                                   |

## Real model acceptance

A real equipment-lending project used a $200 prototype cap, one volunteer developer, six weeks, a private laptop browser workflow, existing inventory CSV, no university API/SSO and an explicit robotics exclusion. Actual intake, broad research and six interview answers were saved. The interview was ended early after six answers; this is not evidence of a hard six-question cap.

The first pre-fix concept request failed after two invalid responses. Its exact invalid field was not retained. Compact rough-output instructions, low reasoning, a shared 145-second deadline, cancellation checks and diagnostic repair reasons replaced the long repeated attempts. A development-server reload interrupted an intermediate retest; acceptance then moved to a built Node preview.

The next real portfolio returned four concepts in 56 seconds. Review found the fourth exceeded the six-week deadline. The prompt now explicitly limits that exception to money, and deterministic relative-duration checks reject over-deadline portfolios and block over-deadline final plans. A subsequent real retest returned in 49 seconds: normal maximum estimates $40/$25/$50, fourth $390; timelines 3–4, 3–5, 4–5 and 5–6 weeks. All citations resolve to supplied evidence; all four preserve the reviewed platform, team, integration, privacy and exclusion constraints. `after/live-portfolio-review.json` records the review.

The selected Local Ledger Desk then exercised a real material conflict: locally editable SQLite files cannot guarantee immutable audit events. The feature editor gained an Edit details control. Revising that promise to application-enforced append-only history preserved the intended workflow and cleared the researched conflict. Research then advanced automatically to final-plan generation, which returned HTTP 200 in 34 seconds. The plan estimates $0–$80 and 4–6 weeks. These are model planning assumptions, not proven costs or schedules.

The final score returned HTTP 200. A 20-page PDF downloaded through the browser; extracted text matches all five confirmed features, the revised description, budget and timeline. See `after/live-final-project.json`, `finish-live-report.pdf`, `live-pdf-consistency.json`, and `finish-live-results-1440.png`. Broad and focused research are honestly marked partial, with gaps retained. The first final timeline contained a model-truncated explanatory sentence; the range was intact. Generation instructions now request a short complete range and put phase details in developmentPhases. A real retest against the deployed endpoint returned HTTP 200 in 37 seconds with the complete `4–6 weeks` timeline and a revised $0–$30 estimate. Research was reused rather than charged again.

Relative-duration validation covers a single explicit day/week/month duration or range. Calendar dates, ambiguous multi-phase prose and semantic constraints still require the model/research assessment; it is not a universal proof of feasibility.

## Motion and visual evidence

Evidence is local under `artifacts/workflow/after/`. The prior working log is retained in `artifacts/workflow/implementation-history.md`.

- Real-time printer captures at 1024×768, 1440×900 and 1920×1080 show the completion cue, physical sheet feed, reach and continuous handoff: `printer-realtime-1024.mp4`, `printer-realtime-1440.mp4`, `printer-realtime-1920.mp4`.
- `final-printer-realtime-1440.mp4` shows the corrected final cover and automatic results transition. Broad/final clock and media pause JSON records hold exact elapsed time while Menu is open.
- `concept-monitor-realtime-1440.mp4` shows Purl, the mail arrivals, continuous monitor grip and swivel. Sampled grip error is at most 0.003 scene units. Entry/skip/reduced-motion checks cover the other sizes; a clean 1024 full sequence is retained separately.
- Cursed attachment recording and narrow/desktop captures show the contained three-window effect, readable settled message, immediate dismissal, Escape/focus return and no replay.
- `results-reveal-clean-1440.webm` shows sequential factors, modifiers, stamp and celebration. Score failure still allowed a nine-page PDF download. Playback instrumentation observed count/modifier/stamp cues; these recordings do not capture audio and are not a human listening test.
- The three media eras across four sizes have no horizontal document overflow, broken images or duplicate visible thumbnails in `media-layout-checks.json`. Native play/seek/next, saved selection, inactive pause and failed-clip recovery were exercised. Pixel-rendered media frames received a final refresh.
- Final journal, inbox, feature editor, finalization and results screenshots use the shared pixel style. The visual correction does not replace the existing room or Sage.

Invalid early CLI recordings of roughly one second are development failures, not acceptance evidence. Use the explicitly named real-time display captures for timing.

## Final checks

The latest full unit suite passes 180 tests across 43 files. Svelte check reports zero errors/warnings. Full lint and the production container build pass. Browser failure/refresh/retry/cancellation fixtures retain completed research and reject a late cancelled plan. Public verification checks DNS, health, authentication, container hardening and the existing tunnel. All 38 new media/cursor/frame/font asset hashes match the local files.

## Public completion

The complete public token-free demo, refreshed session and PDF export passed. A real final-plan request to the deployed app also passed. Both public PDFs match their plans, with nine pages for the demo and twenty for the real project. Fresh public page-error and console logs are empty. See `after/public-pdf-checks.json`, `public-complete-demo.json`, `public-real-complete.json` and the public screenshots/recordings in the verification record. The original 68.75-second opening capture is named `finish-demo-opening-1440.mp4`; it is not claimed as a whole-journey recording.

The functional redesign was deployed on September 13. Its earlier claim of complete presentation acceptance was too broad. The follow-up presentation restoration record tracks the Sage, era progression, and media rendering corrections. The functional limits remain model-estimate uncertainty, explicitly partial research, scoped responsive/race coverage, and playback-call verification rather than a human listening test.
